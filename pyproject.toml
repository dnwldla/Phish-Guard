[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "PhishGuard"
authors = [{name = "JangBoGeun", email = "bogeun0419@gmail.com"}]
dynamic = ["version", "description"]